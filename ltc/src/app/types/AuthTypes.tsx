export interface LoginType {
  email: string;
  password: string;
}

//do i even need this anymore? It was there to check type but not working as expected
export interface RegistrationType {
  email: string;
  password: string;
}
